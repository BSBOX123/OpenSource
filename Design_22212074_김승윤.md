# Survive The Semester (학기 생존기)

### 3. Design Document

- 1 -
Yeungnam University

---

## [ Revision history ]

| Revision date | Version # | Description | Author |
| :---: | :---: | :--- | :---: |
| 2026.04.28 | 1.0.0 | Design 문서 초안 작성 (다이어그램 텍스트 명세화) | Seungyun-Kim |


- 2 -
Yeungnam University

---

## = Contents =

* 1. Introduction ......................................................... 4
* 2. Class Diagram ........................................................ 5
* 3. Sequence Diagram ..................................................... 6
* 4. State Machine Diagram ............................................... 10
* 5. Implementation Requirements ......................................... 11
* 6. Glossary ............................................................ 11
* 7. References .......................................................... 12

- 3 -
Yeungnam University

---

## 1. Introduction

일반적으로 대학생들은 학기 중 쏟아지는 과제, 전공 공부, 어학 준비 등 수많은 일정에 치여 살아간다. 계획을 세우고 실천하려 노력하지만, 대부분의 학생들은 '귀차니즘'과 동기 부여 부족으로 인해 작심삼일에 그치는 경우가 많다. 기존의 체크리스트 형태의 'To-do 앱'들은 일정을 단순히 텍스트로 나열할 뿐, 사용자의 자발적인 참여를 이끌어내는 즉각적인 보상 체계가 부족하다는 한계가 있다.

따라서 본 프로젝트인 "Survive The Semester(학기 생존기)"는 과제 수행을 몬스터 사냥과 생존이라는 RPG 게임 메커니즘으로 치환하여 이 문제를 해결하고자 한다. 사용자가 일정을 완료하면 즉각적인 가상 재화(EXP, Gold)를 획득하고, 마감일을 넘기면 체력(HP)이 깎이는 페널티를 부여함으로써 능동적이고 효율적인 일정 관리를 유도한다. 

본 문서는 Analysis 단계에 이은 Design 단계의 문서로, 구현 전 시스템의 정적, 동적 구조를 설계한다. 본 문서에서는 시각적 다이어그램을 배제하고, 클래스 구조(Class Diagram), 순차적 상호작용(Sequence Diagram), 상태 변화(State Machine Diagram)에 대한 상세한 텍스트 명세를 기술한다.

- 4 -
Yeungnam University

---

## 2. Class Diagram

본 시스템은 유지보수와 모듈화를 위해 MVC(Model-View-Controller) 아키텍처 패턴을 기반으로 설계되었으며, 크게 View(UI), Controller/Manager(로직), Model/Database(데이터) 계층으로 나뉜다.

### 2.1 View Layer (UI 계층)
* **MainActivity**: 메인 화면을 담당하는 안드로이드 액티비티 클래스이다. 아바타 상태바, 우선순위 퀘스트 리스트를 렌더링하며, 사용자의 입력(완료, 추가, 삭제 등)을 캡처하여 `EventController`로 전달한다.
* **ShopActivity**: 상점 화면을 담당한다. 등록된 셀프 보상 아이템 목록을 표시하고, 사용자의 구매 시도를 `EventController`로 전달하여 처리 결과를 화면에 애니메이션으로 렌더링한다.

### 2.2 Controller & Logic Layer (제어 및 연산 계층)
* **EventController**: 뷰와 모델 사이의 중심 중개자 역할을 한다. 뷰의 요청을 받아 적절한 매니저 클래스나 데이터 컨트롤러에 로직 처리를 위임하고, 결과를 취합하여 다시 뷰로 반환한다.
* **QueueManager**: 진행 중인 퀘스트 리스트를 받아 '마감일'과 '난이도'를 기준으로 우선순위 점수를 수학적으로 계산한다. 계산된 결과를 바탕으로 가장 시급한 과제가 최상단에 오도록 `PriorityQueue`로 정렬하여 반환하는 핵심 알고리즘 클래스이다.
* **RewardEngine**: 퀘스트 성공 시 지급할 EXP와 Gold를 연산하고, 마감일 초과 시 차감할 HP 페널티를 계산한다. 또한 현재 경험치를 바탕으로 레벨업 달성 여부를 판별한다.
* **BackgroundScheduler**: 앱이 종료된 상태에서도 시스템의 AlarmManager와 연동하여 동작하는 백그라운드 서비스 클래스이다. 매일 자정 일일 퀘스트를 생성하고, 주기적으로 마감일 초과 퀘스트를 탐지하여 페널티 로직을 트리거한다.

### 2.3 Data Layer (데이터 및 엔티티 계층)
* **DataController**: 안드로이드 내장 SQLite DB와의 접근을 전담하는 레포지토리 클래스이다. DB Query(INSERT, SELECT, UPDATE, DELETE)를 실행하여 영속성을 보장한다.
* **Quest**: 퀘스트의 식별자(ID), 과제명, 마감일, 난이도, 상태 정보를 담고 있는 데이터 엔티티 클래스이다.
* **Player**: 사용자의 현재 레벨, 체력(HP), 경험치(EXP), 골드(Gold) 정보를 담고 있는 데이터 엔티티 클래스이다.

- 5 -
Yeungnam University

---

## 3. Sequence Diagram

객체 간의 동적 상호작용과 시간적 흐름을 정의하는 시퀀스 명세이다.

### 3.1 Register Quest (메인 퀘스트 등록)
사용자가 메인 화면에서 새로운 과제를 등록하는 과정이다.
1. 사용자가 메인 화면의 '+' 버튼을 클릭하여 입력 다이얼로그를 호출한다.
2. 과제명, 마감일, 난이도를 입력하고 등록 버튼을 클릭한다.
3. 뷰가 `EventController`에 데이터를 전달한다.
4. `EventController`는 `DataController`를 호출하여 SQLite DB에 `INSERT` 쿼리를 실행한다.
5. 저장 성공 시, 뷰는 `EventController`를 통해 새롭게 정렬된 퀘스트 목록을 요청하고 화면을 갱신한다.

### 3.2 Modify / Delete Quest (퀘스트 수정 및 삭제)
등록된 퀘스트 정보를 수정하거나 삭제하는 과정이다.
1. 사용자가 리스트 내 특정 퀘스트 항목을 길게 눌러(Long Click) 조작 메뉴를 띄운다.
2. **수정 시**: 변경할 데이터를 입력하고 저장하면, `EventController`가 `DataController`에 `UPDATE` 쿼리를 지시한다.
3. **삭제 시**: 삭제 확인을 누르면, `EventController`가 `DataController`에 `DELETE` 쿼리를 지시한다.
4. 트랜잭션이 완료되면 뷰는 정렬된 리스트를 재요청하여 화면에 반영한다.

### 3.3 Complete Quest (퀘스트 완료 처리)
퀘스트를 완수하고 보상을 획득하는 과정이다.
1. 사용자가 특정 퀘스트의 '완료' 버튼을 클릭한다.
2. `EventController`가 `RewardEngine`에 난이도를 전달하여 보상(EXP, Gold)을 계산하도록 요청한다.
3. `RewardEngine`은 획득 경험치를 바탕으로 레벨업 조건 충족 여부를 판별하여 반환한다.
4. `EventController`는 `DataController`를 통해 퀘스트 상태를 완료(DONE)로 변경하고 플레이어의 스탯을 `UPDATE` 한다.
5. 뷰는 해당 퀘스트를 리스트에서 지우고 스탯 바를 증가시킨다. (레벨업 시 아바타 진화 애니메이션 실행)

- 6 -
Yeungnam University

---

### 3.4 View Quest List (정렬된 퀘스트 목록 조회)
우선순위 알고리즘을 통해 퀘스트를 화면에 나열하는 과정이다.
1. 앱이 실행되거나 데이터 갱신 이벤트가 발생하면 뷰가 `EventController`에 리스트 렌더링을 요청한다.
2. `EventController`가 `DataController`를 통해 완료되지 않은 퀘스트 목록을 전부 가져온다.
3. 데이터를 `QueueManager`로 전달하여 마감일 잔여 시간과 난이도 가중치를 합산해 우선순위를 판별한다.
4. `PriorityQueue` 연산을 통해 가장 시급한 순서대로 정렬된 리스트가 뷰로 반환되어 표출된다.

### 3.5 View Avatar Status (아바타 및 스탯 조회)
앱 상단 UI에 플레이어의 상태를 시각화하는 과정이다.
1. 앱 최초 실행 시 뷰가 `EventController`에 스탯 조회를 요청한다.
2. `DataController`가 DB에서 플레이어 정보를 가져온다. (기존 데이터가 없다면 레벨 1, HP 100의 초기값을 생성하여 저장한다.)
3. 반환된 스탯 데이터를 바탕으로 뷰는 레벨에 맞는 아바타 이미지를 로드한다.
4. 최대치 대비 현재 값의 백분율을 계산하여 HP 및 EXP 게이지 바 UI를 렌더링한다.

### 3.6 Buy Item (상점 아이템 구매)
모은 골드로 보상 쿠폰을 구매하는 과정이다.
1. `ShopActivity`에서 사용자가 특정 보상 아이템의 '구매' 버튼을 터치한다.
2. `EventController`가 `DataController`를 통해 현재 보유 골드를 조회하여 아이템 가격과 비교 검증(Validation)을 수행한다.
3. **잔액 부족 시**: 뷰로 거절 이벤트를 보내 "골드가 부족합니다"라는 경고를 띄운다.
4. **구매 가능 시**: 아이템 가격만큼 골드를 차감하는 `UPDATE` 쿼리를 실행하고, 성공 시 쿠폰 발급 애니메이션을 실행한다.

- 7 -
Yeungnam University

---

### 3.7 Generate Daily Quest (일일 퀘스트 자정 갱신)
백그라운드에서 매일 루틴 과제를 자동 생성하는 과정이다.
1. 시스템의 `AlarmManager`가 기기 시간 자정(00:00)을 감지하여 `BackgroundScheduler`를 실행한다.
2. 스케줄러는 `DataController`를 통해 해당 날짜의 일일 퀘스트가 이미 생성되었는지 중복 검사를 수행한다.
3. 존재하지 않는다면 미리 설정된 루틴 퀘스트 객체를 생성하여 DB에 `INSERT` 한다.
4. 작업 완료 후 OS의 `NotificationManager`를 통해 "새로운 일일 퀘스트가 도착했습니다!"라는 푸시 알림을 발송한다.

### 3.8 Apply Penalty (마감일 초과 페널티 적용)
마감일이 지난 퀘스트에 강제적 페널티를 부여하는 과정이다.
1. 앱 기동 시 또는 `BackgroundScheduler`의 주기적 동기화 시점에 `DataController`를 통해 미완료 퀘스트 목록을 가져온다.
2. 스케줄러가 현재 시스템 시간과 각 퀘스트의 마감일을 대조하여 마감일이 지났는지 판별한다.
3. 마감 초과 퀘스트 발견 시 `RewardEngine`을 통해 차감할 HP를 연산한다.
4. DB에 HP 차감 적용 및 해당 퀘스트를 '실패(FAILED)' 상태로 갱신하고 페널티 푸시 알림을 보낸다. (HP가 0 이하가 되면 골드 몰수 등 게임오버 루틴 추가 실행)

### 3.9 Alert Penalty (마감일 임박 경고 알림)
기한이 임박한 퀘스트를 유저에게 사전 경고하는 과정이다.
1. `BackgroundScheduler`가 주기적으로 미완료 퀘스트의 남은 마감 시간을 연산한다.
2. 마감까지 24시간 미만이 남은 퀘스트를 탐지한다.
3. 중복 알림 전송 플래그를 체크한 후, 알림을 보낸 적이 없다면 DB에 알림 발송 완료 처리를 한다.
4. 안드로이드 OS 채널을 통해 "마감일이 하루 남았습니다! HP 차감 주의"라는 푸시 알림을 상단 바에 표출한다.

- 8 -
Yeungnam University

---

## 4. State Machine Diagram

시스템 내 핵심 객체와 UI의 상태 변화 흐름을 정의한 명세이다.

### 4.1 Quest Lifecycle (퀘스트 객체 상태 전이)
퀘스트 데이터가 생성되어 폐기될 때까지의 생명 주기이다.
* **[상태 1] Created**: 유저가 팝업창에서 데이터를 입력하고 등록을 누른 최초 상태. 즉시 DB에 저장되며 `InProgress`로 전이된다.
* **[상태 2] InProgress**: 리스트에 큐 정렬되어 활성화된 상태. 유저의 액션이나 시간의 흐름을 대기한다.
* **[상태 3] Completed**: 유저가 완료 버튼을 누른 상태. EXP와 Gold 연산 후 리스트에서 숨김 처리되어 아카이브 보관된다. (종료 상태)
* **[상태 4] Failed**: 백그라운드 스케줄러가 마감일 초과를 감지한 상태. HP 페널티를 연산한 뒤 리스트에서 제거된다. (종료 상태)
* **[상태 5] Deleted**: 유저가 임의로 삭제 버튼을 누른 상태. DB 레코드에서 영구 삭제된다. (종료 상태)

### 4.2 App View State (애플리케이션 화면 상태 전이)
유저의 터치 조작에 따른 화면 간 이동 흐름이다.
* **[상태 1] Splash View**: 앱 실행 시 로고가 뜨는 초기 로딩 화면. 로컬 DB 로드가 완료되면 `Main Home View`로 진입한다.
* **[상태 2] Main Home View**: 시스템의 중심 허브 화면. 퀘스트 완료 처리나 수정 시 현재 뷰 안에서 데이터만 동기화 및 재렌더링된다. 앱 종료를 누르면 시스템이 종료된다.
* **[상태 3] Add Quest Dialog**: 메인 화면에서 FAB(+) 버튼을 누르면 진입하는 상태. 등록이나 취소 버튼을 누르면 무조건 다시 `Main Home View`로 복귀한다.
* **[상태 4] Shop View**: 상점 아이콘을 클릭하여 진입하는 상태. 구매 행위는 현재 뷰 내에서 스탯을 새로고침하며 진행된다. 뒤로가기 버튼을 누르면 `Main Home View`로 복귀하고, 앱 종료 트리거 발생 시 시스템이 종료된다.

- 9 -
Yeungnam University

---

## 5. Implementation Requirements

본 시스템의 원활한 구현 및 구동을 위한 최소 요구사항이다.

**S/W Requirements**
* **Operating System**: Android 8.0 (API Level 26, Oreo) 이상 (알람 매니저 및 백그라운드 제한 정책 대응)
* **Implementation Language**: Java 또는 Kotlin
* **IDE**: Android Studio 2022.3 (Giraffe) 이상
* **Database**: SQLite (안드로이드 기본 내장 라이브러리 활용)
* **Design Pattern**: MVC (Model-View-Controller) 아키텍처 적용

**H/W Requirements**
* **Platform**: 안드로이드 OS 기반의 스마트폰 (태블릿 제외)
* **CPU**: 쿼드코어 1.4 GHz 이상 (원활한 애니메이션 및 큐 연산 목적)
* **RAM**: 2GB 이상
* **Storage**: 최소 100MB 이상의 여유 공간
* **Network**: 연결 안 되어 있어도 됨 (완벽한 오프라인 스탠드얼론 동작)

## 6. Glossary

* **MVC Pattern**: 시스템을 Model(데이터), View(화면), Controller(제어 로직)로 역할을 분리하여 결합도를 낮추고 유지보수성을 높이는 설계 기법.
* **EventController**: View에서 발생한 유저 이벤트를 캡처하여 각 로직 매니저들에게 작업을 분배하는 두뇌 역할의 클래스.
* **Priority Queue (우선순위 큐)**: 데이터가 큐에 삽입된 순서에 상관없이, 정해진 규칙(마감일, 난이도 연산 점수)에 따라 우선순위가 가장 높은 요소부터 출력되는 자료구조.
* **SQLite**: 기기 내부에 파일 형태로 저장되는 경량화된 로컬 관계형 데이터베이스 시스템.
* **Background Scheduler**: 유저가 앱을 직접 실행해 화면에 띄우고 있지 않은 폰 꺼짐 상태에서도 주기적으로 연산을 수행하는 은밀한 서비스 프로세스.

- 10 -
Yeungnam University

---

## 7. References

1. Android Developers Official Documentation, https://developer.android.com
2. Java Platform, Standard Edition Documentation (Oracle), https://docs.oracle.com/en/java/
3. "Introduction to Algorithms, 3rd Edition" (Thomas H. Cormen) - 우선순위 큐(Priority Queue) 수학적 구현 참조
4. "Android Room & SQLite Database Guide", Android Developers.
5. "새해 계획 얼마나 지켰나?…대학생 70% '작심삼일'", 노컷뉴스, 2014.01.10.
6. "대학생이 꼽은 시간 허비 이유 '1위 귀차니즘'", 서울시티, 2014.02.28.
